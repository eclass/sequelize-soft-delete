language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - "10"
notifications:
  email: false
stages:
  - lint
  - test
  - name: deploy
    if: branch = master
jobs:
  include:
    - stage: lint
      name: eslint
      script: npx eslint .
    - stage: lint
      name: commitlint
      before_script:
        - npm i -g @commitlint/travis-cli
      script: commitlint-travis
    - stage: test
      script: npm t
    - stage: deploy
      script: npx semantic-release
